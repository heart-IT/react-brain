id: RB-E-AI-DEVTOOLS
topic: "AI-assisted development — agent skills, MCP tooling & device automation"
category: ai-devtools
group: ai
status: drafted
confidence: low
platforms: [react, react-native]
options:
  - { name: "agent-skills (Callstack)", tradeoff: "curated skill packs for coding agents — RN best practices, upgrade playbook, brownfield migration, library scaffolding; installs into Claude Code / Cursor / Codex / Gemini CLI; the broadest pack (~1.5k★)" }
  - { name: "react-native-skills (Margelo)", tradeoff: "RN-focused skills (VisionCamera and native-module depth) via `npx skills add margelo/react-native-skills`; younger/smaller than Callstack's" }
  - { name: "agent-device (Callstack)", tradeoff: "agent-native CLI: AI agents drive real iOS/Android/tvOS/macOS/web devices & simulators — a11y snapshots, actions, replayable scripts; v0.20.x (0.20.1, verified vs npm 2026-07-28), iterating fast — 0.20 redesigned gesture execution and added Maestro compatibility through a typed engine" }
  - { name: "Argent (Software Mansion)", tradeoff: "MCP server + skills: simulator control, debugger attach (React tree + JS), console/HTTP monitoring, correlated React+iOS profiling; 0.17.0 (verified vs npm 2026-07-28) extends beyond iOS — tvOS/Android TV/Vega OS, cloud agents for autonomous bug repro, Argent Lens design review; also listed in RB-E-TESTING" }
  - { name: "Maestro MCP / Maestro Viewer", tradeoff: "agents drive devices and GENERATE E2E flows on the tool the ecosystem already tests with — see RB-E-TESTING (which owns the testing depth)" }
  - { name: "Reactotron MCP", tradeoff: "the classic RN debugger exposes runtime state/network to coding agents via MCP (reactotron-core-server 3.3.0, 2026-05)" }
  - { name: "Expo AI tooling", tradeoff: "SDK 55+ ships agent-focused tooling (Expo skills at docs.expo.dev/skills, MCP integration) for Claude/Cursor/Codex as part of the framework — THIS is the durable part. Expo Agent, their hosted web app-building agent, is DEAD: closed beta ended, agent.expo.dev goes away after 2026-07-31 (export your projects), with Expo saying it would rather integrate with the harnesses developers already use than build its own IDE" }
  - { name: "TanStack Intent (ALPHA)", tradeoff: "a DISTRIBUTION mechanism, not a pack: agent skills ship INSIDE npm packages and are discovered via the node_modules dependency graph, version-pinned with staleness detection; rolling out in TanStack DB first" }
  - { name: "React Native Evals (Callstack)", tradeoff: "open-source benchmark suite for WHICH MODEL writes good RN code — task dataset by category/library, TS+Bun runner, judge + methodology whitepaper; the model-selection layer under all the tools above" }
  - { name: "ReactBench (Million)", tradeoff: "WHICH MODEL writes good REACT-WEB code — Write React / Fix React tasks from real merged PRs, graded by behavioral tests + react-doctor's 400+ deterministic rules (see RB-E-DX) in clean-room containers; public leaderboard (2026-07: GPT 5.6 Terramax/Solmax 53%, Fable 5xhigh 46% Pass@1); the web counterpart to RN Evals" }
recommend:
  default: "Give a coding agent two things: RN KNOWLEDGE — a curated skills pack (Callstack agent-skills is the broadest; add Margelo react-native-skills for camera/native depth) — and EYES+HANDS on the running app — Maestro MCP if you already test with Maestro (see RB-E-TESTING), else agent-device (cross-platform) or Argent (iOS debug/profile depth). Add Reactotron MCP when the agent needs runtime state. Fast-moving (confidence: low) — most of these tools are months old; trial on one workflow before standardizing."
  when:
    - "agent writes RN code → install a skills pack (agent-skills / react-native-skills) so it stops generating stale patterns"
    - "agent must run and SEE the app (closed verify loop) → agent-device (cross-platform) or Argent (iOS debugger/profiler depth)"
    - "agent-generated E2E tests → Maestro MCP (owned by RB-E-TESTING)"
    - "agent needs runtime state / network inspection → Reactotron MCP"
    - "how to WORK with agents (TDD, review discipline, cognitive debt) → agentic-engineering-patterns"
note: >
  NEW entry (2026-07-09): the dominant recurring theme of Native Weekly issues 11–16 (Jan–Jun
  2026) — every single issue shipped agent-tooling news (agent-device ×3, two skills packs,
  Reactotron MCP, Argent, Expo SDK 55/56 AI tooling, Xcode 26.3 'agentic coding') — and the
  corpus had no home for the SELECTION question 'which agent tooling for RN development'.
  Distinct from RB-E-AI-UI (AI in the product) and RB-E-ONDEVICE-AI (models on device): this
  is AI at DEV time. Device-driving TESTING tools (Maestro MCP, Argent, Radon IDE) stay owned
  by RB-E-TESTING; this entry is the cross-tool map. All options fetch-verified 2026-07-09
  (GitHub repos, npm, SWM blog). Everything here is pre-1.0-culture — pin versions, expect churn.
  FIRST CASUALTY (2026-07-28, verified vs the Expo changelog): Expo Agent — carried here since
  2026-07-09 as "waitlist beta … track, don't bet" — was wound down on 2026-07-20, five weeks
  after this entry first listed it; the hosted agent at agent.expo.dev stops working after
  2026-07-31. Expo's stated reasoning is the useful signal, not the shutdown: rather than build a
  web-based harness and IDE, integrate with the harnesses developers already customize deeply.
  That is the shape of this whole category — the SKILLS/MCP layer (portable, rides whatever
  harness you use) outlives the hosted-product layer. Weight option rows accordingly.
  BENCHMARK LAYER (2026-08-07, verified vs appcontrolbench.swmansion.com): SWM Labs'
  AppControlBench compares agent-tool × model configurations (Argent and agent-device — the two
  device-driving options this entry tracks — against Claude/GPT lines) on real iOS app-control
  tasks: ~120 configs with completion/time/cost leaderboards (best on record 2026-08-03: 98%
  completion at $0.22/run, Claude Haiku + Argent 0.15). The selection question this entry owns
  now has a live first-party leaderboard — check it before pinning a tool+model pair.
defer_to_skill: agentic-engineering-patterns
doc: null
updated: 2026-08-07   # AppControlBench leaderboard + Expo model-shootout reading (TWiR #293 + firsthand)
reading:
  - title: "Bun is being rewritten in Rust"
    url: "https://bun.com/blog/bun-in-rust"
    by: "Jarred Sumner (Bun)"
    what: "The largest published agent-driven port to date, and the methodology reading for agent fleets: Bun v1.4 ships a Zig→Rust rewrite of 535,496 lines executed by ~64 parallel Claude agents across 4 worktrees in ~50 workflows over 11 days (2026-05), ~$165k in API cost vs an estimated blocked engineer-year — with the load-bearing discipline that each implementer agent was paired with TWO adversarial reviewers in separate context windows told to assume the code was wrong. Results: every instrumentable memory leak resolved (one bundler workload: 609MB stable vs 6.7GB before), ~20% smaller binaries, 2–5% faster. Not RN-specific, but the concrete data point for what adversarially-reviewed agent fleets can do to a production codebase — the review-pairing pattern transfers to any repo using the tools in this entry."
  - title: "How Expensify Uses Agent-Device for Mobile Bug Evidence and Profiling"
    url: "https://www.callstack.com/blog/how-expensify-uses-agent-device-for-mobile-bug-evidence-and-profiling"
    by: "Callstack (with Expensify)"
    what: "The first named production-adopter case study for agent-device: agents run Sentry-span measurement loops across branches and drive the React profiler mid-session via react-devtools integration, returning named components and render counts as bug evidence. What agent-in-the-debug-loop looks like at a real company, beyond vendor demos."
  - title: "Metrognome — An AI Agent for Measured React Native Performance Fixes"
    url: "https://www.callstack.com/blog/an-ai-agent-for-measured-react-native-performance-fixes"
    by: "Callstack"
    what: "The layer ABOVE the device-drivers in this entry: not another profiler or benchmark, but a loop that decides what to keep. For a given performance goal it proposes one hypothesis, runs the experiment, measures it N times on a live app, keeps the change only if it beats the noise, commits it with the evidence, then picks the next hypothesis — one variable at a time. It ships no profiler and no device driver of its own; it orchestrates the tools already listed here (Agent Device for eyes and hands, Agent React DevTools, and the rest of Callstack's stack) and contributes the research loop plus memory. VENDOR POST — nearly every organ carries a Callstack logo, and the numbers are theirs. The durable, transferable part is the discipline: an optimization agent that can REJECT its own work against a noise floor is a different thing from one that reports a diff."
    claim: "You point an agent at performance work — require it to measure N times against a noise floor and reject its own changes that don't beat it; an agent that only reports a diff has no way to tell an optimization from a coincidence."
    applies_when: { platforms: [react-native], stages: [production, scale] }
  - title: "Meet Argent: Agentic Toolkit to Control, Debug and Profile iOS applications"
    url: "https://swmansion.com/blog/argent-agentic-toolkit-to-control-debug-and-profile-ios-applications/"
    by: "Kacper Kapuściak (Software Mansion)"
    what: "The clearest articulation of the closed loop this entry is about: the agent that writes the code also boots the simulator, drives the UI, attaches a debugger, and profiles React+iOS in the same session — with production numbers (~50% re-render reduction in a banking app) rather than demos."
  - title: "Fable 5 vs GPT-5.6 Sol: I spent $2,000 and 2 billion tokens to find out who wins"
    url: "https://expo.dev/blog/fable-5-vs-gpt-5-6-sol-expo-apps"
    by: "Rami Maalouf (Expo)"
    what: "First-party measured model comparison on real Expo apps: three models (Fable 5/Claude Code, GPT-5.6 Sol/Codex, GPT-5.5 control) each one-shot three apps end-to-end — ideation, per-feature on-simulator validation, debugging — under the same spec-driven prompt and template. Fable 5 won code + UI quality (code-health 88 vs 79/79) at ~3× the hourly cost ($74/h vs ~$30/h) and ~30% less wall-clock; the dominant token cost across all models was the simulator-validation loop, not generation. The model numbers are point-in-time (2026-08-05); the durable part is the harness — standing quality bars plus per-feature simulator validation is what made one-shot apps work at all. (Fetched via browser-UA curl; expo.dev/blog no longer blocks it.)"
    claim: "You drive coding agents against an RN/Expo app — budget for the validation loop, not generation: per-feature on-simulator verification dominates token spend and is what makes one-shot quality possible; pick the model on measured cost-vs-speed, not vibes."
    applies_when: { deps: ["expo"], platforms: [react-native] }
sources:
  - "https://appcontrolbench.swmansion.com/"
  - "https://www.reactbench.com/blog"
  - "https://expo.dev/changelog/expo-agent-ending-the-closed-beta-and-winding-the-project-down"
  - "https://swmansion.com/blog/closing-the-agent-loop-autonomous-react-native-bug-reproduction-with-argent/"
  - "https://github.com/callstackincubator/agent-skills"
  - "https://github.com/margelo/react-native-skills"
  - "https://github.com/infinitered/reactotron/releases/tag/reactotron-core-server%403.3.0"
  - "https://github.com/callstack/agent-device"
  - "https://tanstack.com/blog/from-docs-to-agents"
  - "https://www.callstack.com/blog/announcing-react-native-evals"
detect:   # package.json dep → this entry (pkg: exact | @scope/* | name*; token matched against recommend text)
  - { pkg: "agent-device", label: "agent-device", token: "agent-device" }
  - { pkg: "reactotron-react-native", label: "Reactotron", token: "reactotron" }
  - { pkg: "@swmansion/argent", label: "Argent", token: "argent" }
