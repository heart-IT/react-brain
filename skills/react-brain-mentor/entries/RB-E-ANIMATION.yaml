id: RB-E-ANIMATION
topic: "Animation & gestures"
category: animation
group: ui
status: reviewed   # long-form entry written + source-checked → encyclopedia/RB-E-ANIMATION.md
confidence: medium
platforms: [react, react-native]
options:
  - { name: "Reanimated 4", tradeoff: "UI-thread + CSS-style animations/transitions; worklets split into react-native-worklets; requires New Arch" }
  - { name: "Gesture Handler 3.0", tradeoff: "native gestures; v3 adds a hook-based API built for the React Compiler + Reanimated 4" }
  - { name: "react-native-worklets (0.11.x)", tradeoff: "the worklets runtime under Reanimated 4 (now its own package; npm latest 0.11.4, verified 2026-08-18 — an earlier '8.0' claim here was WRONG, caught by the firsthand watch); 0.11 adds experimental Hermes-bytecode worklets + Shareable values across isolated runtimes. 0.12.0 shipped 2026-08-11 but sits on the `next` dist-tag, NOT latest (WeakRef on worklet runtimes; Bundle Mode script loading on par with RN's own loaders) — read it as the next line, not the one to install. TURN ON BUNDLE MODE (stable since 0.10) — below RN 0.87 it is the fix for the Hermes V1 memory regression, and worth it regardless; see note" }
  - { name: "react-native-skia (2.x)", tradeoff: "GPU 2D drawing; v2.1 plays Lottie via Skottie; pairs with Reanimated" }
  - { name: "lottie-react-native", tradeoff: "designer Lottie/JSON animations; v7 adds visionOS" }
  - { name: "Rive — @rive-app/react-native (Nitro) / rive-react-native (legacy)", tradeoff: "designer-driven INTERACTIVE vector animation (state machines) — the Lottie alternative; the SDK was rewritten on Nitro under Rive's official scope (@rive-app/react-native, 0.4.x EARLY line): files/view-models become shared typed HybridObjects — Margelo-measured up to 94× multi-view loads + ~4.7× lower memory vs legacy (rewrite authors' own benchmark); legacy unscoped pkg at 9.8.3, quiet since 2026-04" }
  - { name: "react-native-morph-view (Software Mansion)", tradeoff: "GPU image-morph transitions (Metal/AGSL shaders, metaball blend); RN 0.76+, iOS 15.1+/Android 13+; niche/new" }
  - { name: "Animated (core)", tradeoff: "built-in; RN 0.85 shared backend allows layout-prop animation with the native driver (from 0.85.1)" }
  - { name: "Moti / LayoutAnimation", tradeoff: "declarative transitions; Moti wraps Reanimated — but it is DORMANT, not merely slow: npm 0.30.0 published 2025-01-29 and the repo has taken no commits since 2025-03-11 (verified 2026-08-18), while its own dependency list still pins framer-motion ^6.5.1 and its react-native-reanimated peer is a wildcard, so nothing warns you on Reanimated 4. Fine if already installed and working; do NOT start new work on it — plain Reanimated or LayoutAnimation is the live path" }
  - { name: "react-native-screen-transitions", tradeoff: "custom screen/shared-element transitions FOR React Navigation (blank-stack navigator, bounds-driven navigation.zoom(), gesture choreography); featured on the official React Navigation blog; v3.8 active 2026-07, newer" }
  - { name: "react-native-ease", tradeoff: "animations driven entirely by Core Animation/ObjectAnimator — no per-frame JS OR UI-thread worklet work; author-run benchmark shows it holding frame budget at view counts where Reanimated saturates; young, and see the benchmark-bias caveat in reading" }
  - { name: "Motion / framer-motion (WEB)", tradeoff: "the web (React DOM) animation default — declarative layout/exit/gesture animation; Motion is framer-motion's successor; React Native uses Reanimated instead" }
recommend:
  default: "React Native → Reanimated 4 + Gesture Handler 3 for real UI-thread animation & gestures (core Animated only for trivial cases). Web (React DOM) → Motion (framer-motion's successor) for declarative layout/exit/gesture animation."
  when:
    - "web animation (React DOM) → Motion / framer-motion — Reanimated & Gesture Handler are React Native only"
    - "custom/GPU drawing or visuals → react-native-skia"
    - "designer-made animations → lottie-react-native (or Skia/Skottie)"
    - "designer animations that need INTERACTIVITY/state machines, or many Rive views sharing files → Rive; new work goes to the Nitro line @rive-app/react-native (0.4.x, early — pin), not the quiet legacy package"
    - "image-to-image morph transition → react-native-morph-view (GPU shader; niche, lead-level)"
    - "simple fade/slide, want declarative → LayoutAnimation or a Reanimated entering/exiting preset (RN); Motion (web). Moti reads as the ergonomic choice here but is dormant — see the option row"
    - "custom screen / shared-element transitions on React Navigation → react-native-screen-transitions (new; pin)"
    - "hundreds of concurrently-animating views blowing the frame budget → benchmark react-native-ease (fully native-driven; young)"
    - "heavy animation perf → route to react-native-best-practices"
note: >
  RN 0.85 (2026-04) introduced a shared C++ animation backend so Animated and Reanimated
  ride one engine (built with Software Mansion). Heavy-animation perf routes to
  react-native-best-practices. RIVE (added 2026-07-13, Margelo blog pass): the RN SDK's
  Nitro rewrite ships as NEW scoped @rive-app/react-native (0.4.14) while legacy
  rive-react-native sits at 9.8.3 (both verified vs npm 2026-07-13) — a supersession in
  progress; WATCH for the new line reaching 1.0 or a formal legacy deprecation before
  adding a migrate rule (wired as tripwires 2026-07-16 — firsthand fires when either lands). WEB: Motion (the framer-motion successor) is the web
  animation default — added 2026-06-25 after the evidence-loop corpus self-audit flagged
  that this [react]-platform entry had options/recommend for React Native only, no web pick
  (caught from ledgerhr's framer-motion usage). Verified vs the official RN 0.85 blog.
  HERMES V1 WORKLET MEMORY REGRESSION — FIXED IN RN 0.87 (history, kept because it still bites
  every app below 0.87; original finding 2026-07-28, verified vs the Software Mansion writeup and
  reproduced independently in the Calazans benchmark below): Hermes V1 attached ~512KB of debug
  metadata to EVERY eval'd worklet, and Worklets' Legacy Eval Mode ships animation code to the UI
  runtime as strings — so memory scaled with unique worklets triggered. Software Mansion measured
  Expensify's bundle at 1,000+ unique worklets, with 100+ evaluated just to initialize (≥50MB at
  startup). The Hermes fix was backported to 250829098.0.15, and RN 0.87 (2026-08-11) pins
  hermes-compiler 250829098.0.16 — verified vs the v0.87.0 tag's package.json — so the regression
  is closed from 0.87 up. BELOW 0.87 the ladder still applies, in preference order: turn on
  WORKLETS BUNDLE MODE (exposes the mmap'd bytecode bundle instead of eval'ing strings — measured
  −~100MB PSS, ~45%, native heap ~142MB→~32MB, with frame rate and CPU unchanged), the
  experimental bytecode option for Legacy Eval Mode, or pin Hermes 250829098.0.15. TURN ON BUNDLE
  MODE REGARDLESS: SWM still recommends it for its own performance gains, and Worklets 0.12
  (2026-08-11) brought its script loading to parity with React Native's own loaders — local
  bundles are mmapped instead of held in an in-memory std::string for the app's lifetime, and
  Android dev-server bundles stream to a cache file (verified vs the worklets-0.12.0 release).
  SKIA 2.10 MOVED OFF HostObject (2026-07-23, verified vs the release + the published manifest):
  the release's one feature is "migrate from host objects to native states" (#3964) — Skia's JSI
  objects now use NativeState rather than HostObject, the direction the Hermes team favors because
  HostObject routes every property access back through a C++ trampoline. Its published peer range
  is react-native >= 0.78 and react >= 19.0, with react-native-reanimated >= 4.0.0 and
  react-native-worklets >= 0.7.0 as OPTIONAL peers — i.e. Skia does not force Reanimated on you,
  but if you pair them the v4 line is the floor.
  MOTION 13 (web; 2026-08-06, verified vs npm + the motion.dev upgrade guide): one breaking
  change — drops @emotion/is-prop-valid as an auto-detected optional dependency, so
  styled-components/Emotion codebases must inject the prop-validation function via MotionConfig
  (or invert composition so the styling library controls prop forwarding) or previously-filtered
  props leak onto the DOM. framer-motion (the legacy package name) moved to 13.0.0 in lockstep;
  neither package is deprecated.
defer_to_skill: react-native-best-practices
doc: "RB-E-ANIMATION.md"   # long-form Explanation entry in content_dir
updated: 2026-08-18   # challenged → WEAKENED (Moti when-clause contradicted its own caveat); Hermes tripwire closed in RN 0.87
reading:
  - title: "CSS vs. JavaScript Animations"
    url: "https://www.joshwcomeau.com/animation/css-vs-javascript/"
    by: "Josh W. Comeau"
    what: "First-principles explainer on the real distinction (main-thread contention, compositor-friendly properties, WAAPI) rather than the CSS-is-always-faster myth. A durable animation mental model that applies to web and RN reasoning alike."
  - title: "How react-native-ease runs animations with no JavaScript loop"
    url: "https://www.peterp.me/articles/how-react-native-ease-runs-animations-with-no-javascript-loop/"
    by: "Peter Piekarczyk"
    what: "Deep dive on driving animations entirely via Core Animation / ObjectAnimator with no per-frame JS, incl. iOS model vs presentation layers and cross-platform spring physics. The mental model for truly native-driven RN animation."
    claim: "You ship react-native-ease — animations run entirely via Core Animation/ObjectAnimator with no per-frame JS or UI-thread worklet work; the mental model is iOS model vs presentation layers plus cross-platform spring physics."
    applies_when: { deps: ["react-native-ease"], platforms: [react-native] }
  - title: "Chasing a Phantom Jump: making Skia + Reanimated smooth on low-end Android"
    url: "https://blog.margelo.com/profiling-skia-reanimated-low-end-android"
    by: "Vishesh Raheja (Margelo)"
    what: "A low-end-Android profiling deep-dive: smoothness is frame-cadence timing, not curve math; the dominant fix is SurfaceView over TextureView (no per-frame texture upload, ~50-65% less RenderThread CPU), plus stable-reference buffer mutation and quantizing per-frame values. With profiler-triangulation methodology (gfxinfo/Perfetto/Hermes)."
    claim: "You ship Skia — on low-end Android the dominant smoothness fix is SurfaceView over TextureView (no per-frame texture upload, ~50-65% less RenderThread CPU); smoothness is frame-cadence timing, not curve math."
    applies_when: { deps: ["@shopify/react-native-skia"], platforms: [react-native] }
  - title: "The real cost of React Native animations: benchmarking every approach"
    url: "https://expo.dev/blog/the-real-cost-of-react-native-animations-benchmarking-every-approach"
    by: "Janic Duplessis (Expo; react-native-ease author)"
    what: "The only cross-library per-frame benchmark: UI-thread cost of core Animated vs Reanimated (shared values AND the CSS API) vs Ease on real devices — at ~500 animating views only the fully native-driven approach stays under frame budget (Reanimated SVs measured ~36ms/frame). CAVEAT: the author created Ease, the approach that wins — treat the numbers as vendor-run and re-measure on your own screens; the methodology and the per-frame cost model are the durable part."
    claim: "You ship Reanimated — the only cross-library per-frame benchmark measured its shared values at ~36ms/frame at ~500 animating views; only the fully native-driven approach stayed under budget (vendor-run by the Ease author — re-measure)."
    applies_when: { deps: ["react-native-reanimated"], platforms: [react-native] }
  - title: "Which React Native Animation Library Should You Use for Performance?"
    url: "https://andrei-calazans.com/posts/2026-07-15-which-react-native-animation-library/"
    by: "Andrei Calazans"
    what: "The INDEPENDENT replication the Expo/Ease benchmark above asks for — same three-way question (Animated with useNativeDriver, Reanimated 4.5, Ease 0.7), different author, reproducible harness (AndreiCalazans/react-native-animation-performance), Android release build on New Arch, 90Hz physical device, Maestro-driven, median of 3, 60 boxes animating. It splits the answer BY ANIMATION TYPE instead of picking a winner: for touch/state/loop all three keep JS at the idle floor and Ease drops the fewest frames with Animated close behind; SCRUB is the fork — Animated drives the value via PanResponder→setValue on the JS thread (29% JS CPU, ~26% of JS frames dropped, and useNativeDriver cannot save a value you set from JS each frame) while Reanimated keeps it entirely on the UI thread (2.9% JS CPU). Blunt version: Reanimated when a finger is dragging, Ease or the native driver otherwise. Two caveats the author states himself: 60 independent nodes deliberately punishes per-node cost (Reanimated's useAnimatedStyle mapper), so the UI-thread loop ranking is partly an artifact — at one hero element Reanimated is likely best overall; and the ~50MB memory premium it measured DISAPPEARS with Worklets Bundle Mode on (−~100MB PSS in the follow-up re-run)."
    claim: "You ship Reanimated — independently measured, it is the only library keeping continuous gesture input off the JS thread (2.9% vs Animated's 29% JS CPU while scrubbing), but its per-node UI-thread cost dominates when many nodes animate at once."
    applies_when: { deps: ["react-native-reanimated", "react-native-ease"], platforms: [react-native] }
  - title: "A React trick to improve exit animations"
    url: "https://barvian.me/react-exit-animations"
    by: "Max Barvian"
    what: "Uses Suspense to freeze exiting content during animations; explains Fragment refs, useInsertionEffect, and Suspense DOM-update behavior. A durable web exit-animation technique grounded in React internals."
  - title: "Rewriting Rive React Native with Nitro Modules: up to 94× Faster Multi-View Loads"
    url: "https://blog.margelo.com/rewriting-rive-react-native-with-nitro-modules"
    by: "Miklós Fazekas (Margelo)"
    what: "Why the Rive RN SDK's wins came from MODELING, not micro-optimization: the legacy module flattened files/view-models into a per-view God object, so every view re-parsed state; Nitro HybridObjects make them shared typed objects (parse once, render many) — 94× on a 24-view load, ~4.7× lower memory, ~0.3-0.4µs calls vs ~1.5µs TurboModule / ~21µs legacy bridge (iPhone 13 mini, release; the rewrite authors' own benchmark). A general lesson for native-module design: model objects, not modules."
    claim: "You ship Rive — the Nitro line (@rive-app/react-native, 0.4.x early) makes files/view-models shared typed objects: parse once, render many views (94× multi-view loads, ~4.7× lower memory in the authors' benchmark); the legacy unscoped package is quiet."
    applies_when: { deps: ["rive-react-native", "@rive-app/react-native"], platforms: [react-native] }
sources:
  - "https://swmansion.com/blog/how-worklets-bundle-mode-accidentally-fixed-Hermes-v1-memory-regression/"
  - "https://github.com/software-mansion/react-native-reanimated/releases/tag/worklets-0.12.0"
  - "https://reactnative.dev/blog/2026/04/07/react-native-0.85"
  - "https://registry.npmjs.org/@rive-app%2freact-native"
  - "https://reactnavigation.org/blog/2026/04/27/building-custom-screen-transitions"
  - "https://registry.npmjs.org/react-native-screen-transitions/latest"
  - "https://github.com/Shopify/react-native-skia/releases/tag/v2.10.0"
detect:   # package.json dep → this entry (pkg: exact | @scope/* | name*; token matched against recommend text)
  - { pkg: "react-native-reanimated", label: "Reanimated", token: "reanimated" }
  - { pkg: "react-native-gesture-handler", label: "Gesture Handler", token: "gesture handler" }
  - { pkg: "react-native-worklets", label: "Worklets", token: "worklets" }
  - { pkg: "framer-motion", label: "framer-motion / Motion", token: "motion" }
  - { pkg: "motion", label: "Motion", token: "motion" }
  - { pkg: "lottie-react-native", label: "Lottie", token: "lottie" }
  - { pkg: "@shopify/react-native-skia", label: "Skia", token: "skia" }
  - { pkg: "moti", label: "Moti (dormant)", token: "moti" }
  - { pkg: "react-native-screen-transitions", label: "screen-transitions", token: "screen-transitions" }
  - { pkg: "react-native-ease", label: "react-native-ease", token: "react-native-ease" }
  - { pkg: "@rive-app/react-native", label: "Rive (Nitro line)", token: "rive" }
  - { pkg: "rive-react-native", label: "Rive (legacy pkg)", token: "rive" }
detect_source:   # source-level signals owned by this entry (regex over repo source)
  - pattern: "useNativeDriver:\\s*false"
    signal: "JS-driven Animated animations (useNativeDriver: false)"
    hint: "drive animations off the JS thread — native driver where supported, else Reanimated (perf depth → react-native-best-practices)"
tripwires:   # standing caveats as executable conditions — evaluated by `harvest firsthand`; fired ⇒ do the then:, update prose, remove the row
  - when: { pkg: "@rive-app/react-native", atleast: "1.0.0" }
    then: "the Nitro Rive line hit 1.0 — add the rive-react-native → @rive-app/react-native migrate rule the note's WATCH promises, and drop the 'pin, 0.x' caveat from the option row"
    added: 2026-07-16
  - when: { pkg: "rive-react-native", deprecated: true }
    then: "legacy Rive SDK formally deprecated on npm — add the migrate rule (urgency: deprecated) per the note's WATCH; receipts: this flag + the Margelo rewrite post"
    added: 2026-07-16
