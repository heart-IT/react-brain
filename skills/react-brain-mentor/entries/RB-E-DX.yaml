id: RB-E-DX
topic: "Developer experience — CI, lint/format, hooks, monorepo, onboarding"
category: dx
group: tooling-ops
status: reviewed   # long-form entry written + source-checked → encyclopedia/RB-E-DX.md
confidence: high
platforms: [react, react-native]
options:
  - { name: "CI (GitHub Actions / similar)", tradeoff: "gate tests + typecheck + lint + build on every PR; the single highest-leverage DX baseline" }
  - { name: "lint/format — Biome or ESLint (flat) + Prettier", tradeoff: "Biome = one fast Rust tool (lint+format); ESLint flat + Prettier = bigger plugin ecosystem (react-hooks, react-compiler rules)" }
  - { name: "Oxlint + oxlint-tsgolint (type-aware)", tradeoff: "the third lint lane, and the reason to re-evaluate: TYPE-AWARE linting went STABLE 2026-07-22 with tsgolint v7 — tracks TypeScript 7.0.2 and covers 59 of typescript-eslint's 61 type-aware rules (`oxlint --type-aware`). Oxlint 1.79 (2026-08-18) then ported the REACT COMPILER surface: 22 compiler-powered rules using the compiler's own validation passes, recommended-preset rules mapped into Oxlint's correctness category (replacing the old nursery react/react-compiler rule); react-hooks proper remains the ESLint-only piece. 1.83 (2026-09, verified vs the oxc release) updates its lint rules for REACT 19.3, so the Oxlint lane no longer trails the React line this corpus recommends in RB-E-REACT-CORE" }
  - { name: "git hooks — husky + lint-staged / lefthook", tradeoff: "run lint/format/tests pre-commit/pre-push; catch issues before CI" }
  - { name: "monorepo task runner — Turborepo / Nx + pnpm workspaces", tradeoff: "cached, parallel task graph across packages (see RB-E-BUILD)" }
  - { name: "dependency hygiene — renovate/dependabot + audit", tradeoff: "automated updates + npm/pnpm audit + install-script blocking (see RB-E-SECURITY)" }
  - { name: "react-doctor (Million)", tradeoff: "npx react-doctor — one-shot codebase scan for React footguns (state/effects/perf/a11y/security), CI mode + installable as an agent skill; 13.5k★, from the Million.js/react-scan team; positioned as the lint layer for agent-written React (see RB-E-AI-DEVTOOLS)" }
recommend:
  default: "Gate tests + typecheck + lint on every PR in CI, and enforce the same locally with a pre-commit hook. For lint/format, default to Biome (one fast tool) unless you need ESLint's plugin ecosystem (react-hooks, react-compiler)."
  when:
    - "has tests but no CI → add CI FIRST; invariants that aren't gated silently rot (a real finding in audited production apps)"
    - "needs ESLint react-hooks / react-compiler rules → ESLint flat config + Prettier over Biome — but the exclusivity is now react-hooks only: Oxlint 1.79 carries the React Compiler rules natively, and Biome 2.5.8 began closing the same gap (see note)"
    - "kept typescript-eslint ONLY for type-aware rules → re-evaluate: oxlint --type-aware is stable and covers 59/61 of them"
    - "multi-package repo → pnpm workspaces + Turborepo for a cached task graph; pnpm 11.11+ also versions and releases the workspace itself (see note) — no separate changesets tool needed"
    - "normative styleguide but no linter config → wire the rules into lint so they're enforced mechanically, not by review"
    - "lots of agent-written React landing in the repo → add a react-doctor pass (CI or agent skill) on top of lint"
note: >
  COHORT SIGNAL (2026-09-10, `react-brain census` delta, verified by fetching the package.json):
  SENTRY has moved its lint lane — getsentry/sentry now carries oxlint 1.81.0 and NO eslint or
  typescript-eslint at all. This is a real week-over-week change, not a tracking artifact (Oxlint was
  already in the census baseline), and it is the first cohort app on record to drop ESLint outright
  rather than run the two side by side. It does not overturn the react-hooks caveat below — a repo
  can simply accept not having those rules — but it is the strongest adoption datapoint yet that the
  type-aware + compiler-rule surface has become sufficient for a large TypeScript codebase.
  DX is cross-cutting — architecture/boundaries depth → engineering-principles; bundler choice →
  RB-E-BUILD; supply-chain → RB-E-SECURITY. This entry is the feedback loop (CI/lint/hooks/
  onboarding) that keeps the others honest.
  BIOME STARTS ON THE REACT-COMPILER GAP (2026-08-13, verified vs the Biome rule docs): 2.5.8 adds
  `useReactCompiler`, which RUNS React Compiler in lint mode and surfaces its actionable
  diagnostics (e.g. a hook called inside a conditional). It self-scopes — it only runs when the
  nearest package.json declares React ≥19 — but it is in the NURSERY group at `information`
  severity, i.e. experimental and off by default; you opt in per-rule. That narrows, without
  closing, the "ESLint is the only lane with react-hooks/react-compiler rules" reason above:
  treat it as a signal about where Biome is heading, not yet as parity.
  LINT LANDSCAPE SHIFT (2026-07-28, verified vs the Oxc release post): type-aware linting is no
  longer a reason to stay on typescript-eslint — tsgolint v7 shipped stable, tracking TypeScript
  7.0.2 with 59 of typescript-eslint's 61 type-aware rules behind `oxlint --type-aware`. The
  remaining ESLint-only surface for React work NARROWED AGAIN 2026-08-18 (verified vs the Oxc
  blog): Oxlint 1.79 ships 22 React Compiler-powered rules (the compiler's validation passes,
  recommended presets → correctness category), so react-compiler linting is no longer ESLint's
  exclusive either — react-hooks is what the recommend clause above still keys on. Same
  announcement: `oxc-transform-react` applies React Compiler's automatic memoization ~10× faster
  than the Babel plugin in Oxc's preliminary benchmark, wired into @vitejs/plugin-react 6.1.0 —
  the build-side companion for repos adopting the compiler (RB-E-REACT-CORE has the compiler
  default itself).
  MONOREPO RELEASE MANAGEMENT MOVED INTO pnpm (2026-07-28, verified vs the pnpm 11.11-11.14 post
  — this fires the twir-290 reopen signal on the merged pnpm RFC, which said "DX/BUILD candidate
  when it lands in a pnpm release"): `pnpm change` records changesets-compatible `.changeset/*.md`
  intents, a bare `pnpm version -r` consumes them across the workspace with dependent propagation
  through `workspace:` ranges and a committed `.changeset/ledger.yaml` that keeps cherry-picks and
  merge-backs safe, and `pnpm lane` runs per-package prerelease lanes. Config lives under a
  `versioning` key in pnpm-workspace.yaml; changelogs default to REGISTRY storage (composed at
  publish time, no committed CHANGELOG.md) unless you set `versioning.changelog.storage:
  repository`. Same releases add `pnpm doctor` (installation/environment diagnosis with non-zero
  exit, --json, --offline) and fix a path-traversal vulnerability. For a pnpm monorepo this
  removes a dependency (changesets) rather than adding one.
defer_to_skill: engineering-principles
doc: "RB-E-DX.md"   # long-form Explanation entry in content_dir
updated: 2026-09-21   # Oxlint 1.83 updates its rules for React 19.3 — the third lint lane keeps pace with the recommended React line
reading:
  - title: "Best CI/CD for mobile apps in 2026: a practical comparison"
    url: "https://expo.dev/blog/best-ci-cd-for-mobile-apps"
    by: "Expo (VENDOR — comparing its own EAS Workflows)"
    what: "The only structured selection matrix for mobile CI platforms (EAS Workflows vs Bitrise vs Codemagic vs GitHub Actions): fingerprint-based repack turning 10–15min builds into ~2min JS-only builds, and GHA macOS runners measured ~2.4x slower than Codemagic M4s. READ AS A VENDOR DOC — Expo wins its own comparison; the evaluation axes (runner hardware, cache strategy, fingerprint/repack) are the durable part."
  - title: "Expo without EAS: scaling the React Native DX of an app with 90M+ users"
    url: "https://medium.com/doctolib/expo-without-eas-scaling-the-react-native-developer-experience-of-an-app-with-90m-users-2694ea841805"
    by: "Doctolib"
    what: "Large-scale case study adopting Expo's bare workflow (not EAS) with fingerprint caching + JS patching to halve CI builds and grow contributors 9→105. A durable enterprise RN build/CI-DX blueprint."
    claim: "You ship Expo at scale — Doctolib's blueprint runs the bare workflow without EAS: fingerprint caching + JS patching halved CI builds while contributors grew 9→105."
    applies_when: { deps: ["expo"], stages: [scale] }
sources:
  - "https://biomejs.dev/linter/rules/use-react-compiler/"
  - "https://medium.com/doctolib/expo-without-eas-scaling-the-react-native-developer-experience-of-an-app-with-90m-users-2694ea841805"
  - "https://github.com/millionco/react-doctor"
  - "https://oxc.rs/blog/2026-07-22-type-aware-linting-stable"
  - "https://oxc.rs/blog/2026-08-18-react-compiler-support"
  - "https://pnpm.io/blog/releases/11.11-11.14"
detect:   # package.json dep → this entry (pkg: exact | @scope/* | name*; token matched against recommend text)
  - { pkg: "husky", label: "husky (git hooks)", token: "husky" }
  - { pkg: "lefthook", label: "lefthook", token: "lefthook" }
  - { pkg: "@biomejs/biome", label: "Biome", token: "biome" }
  - { pkg: "eslint", label: "ESLint", token: "eslint" }
  - { pkg: "prettier", label: "Prettier", token: "prettier" }
  - { pkg: "lint-staged", label: "lint-staged", token: "lint-staged" }
  - { pkg: "turbo", label: "Turborepo", token: "turborepo" }
  - { pkg: "nx", label: "Nx", token: "nx" }
  - { pkg: "lunte", label: "lunte (lint)", token: "lint" }
  - { pkg: "react-doctor", label: "react-doctor", token: "react-doctor" }
  - { pkg: "oxlint", label: "Oxlint", token: "oxlint" }
  - { pkg: "typescript-eslint", label: "typescript-eslint", token: "type-aware" }
