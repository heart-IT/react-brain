id: RB-E-A11Y
topic: "Accessibility across web & native"
category: accessibility
group: ui
status: reviewed   # long-form entry written + source-checked
confidence: medium
platforms: [react, react-native]
options:
  - { name: "semantic roles/labels", tradeoff: "web ARIA vs RN accessibility props" }
  - { name: "focus management", tradeoff: "keyboard (web) vs screen-reader focus (native)" }
recommend:
  default: "Use semantic roles + labels, manage focus order, and announce dynamic updates; test with role-based queries, not data-testid."
  when:
    - "web → ARIA roles + keyboard focus management"
    - "React Native → accessibilityRole/Label + screen-reader focus"
    - "depth audit (contrast, intrinsics, dynamic type) → design-systems-governance"
defer_to_skill: design-systems-governance
doc: "RB-E-A11Y.md"   # long-form Explanation entry in content_dir
updated: 2026-08-18   # the blocked-aria-hidden ordering rule: focus leaves before the region hides
sources:
  - "https://css-tricks.com/blocked-aria-hidden-fix/"
  - "https://css-tricks.com/the-siren-song-of-arianotify/"
  - "https://certificates.dev/blog/accessibility-in-react-common-mistakes-and-how-to-fix-them"
reading:
  - title: "Blocked aria-hidden: The Warning is Right, and Every Fix You've Found is Wrong"
    url: "https://css-tricks.com/blocked-aria-hidden-fix/"
    by: "Durgesh Rajubhai Pawar (CSS-Tricks)"
    what: "One rule, and the reason the popular fixes are worse than the bug: FOCUS HAS TO LEAVE A REGION BEFORE THAT REGION BECOMES HIDDEN OR INERT. The browser's 'Blocked aria-hidden on an element because its descendant retained focus' warning is telling the truth — a screen-reader user's focus is about to land in a hole — and the three fixes that dominate search results (blur() the active element, wrap the close in setTimeout, or strip the aria-hidden attribute) all silence the console while leaving that user stranded. The fix is an ordering change in code most apps already have: move focus out first, then hide/inert the region, and inert the closing overlay itself (the step nearly everyone skips). The shortcut worth taking when you can: native <dialog> with .showModal() runs the whole focus dance for you — with the one case no browser can guess, restoring focus to an element that has since been removed from the DOM."
    claim: "You hide regions when a modal or drawer closes — move focus OUT before the region becomes hidden or inert (not blur(), not setTimeout, not deleting aria-hidden), and prefer native <dialog>.showModal(), which handles the focus sequence for you."
    applies_when: { platforms: [react] }
  - title: "The Siren Song of ariaNotify()"
    url: "https://css-tricks.com/the-siren-song-of-arianotify/"
    by: "Mat Marquis (CSS-Tricks)"
    what: "The coming primitive for this entry's 'announce dynamic updates' facet: ariaNotify() (WAI-ARIA 1.3) pushes screen-reader announcements from JS — document.ariaNotify(str, {priority}) — replacing the hidden aria-live-region hacks whose timing/support failures the piece catalogs. Two durable caveats: it ships in Firefox ONLY as of mid-2026 (keep aria-live fallbacks), and it invites alert()-style overuse — narrating what existing semantics already convey interrupts screen-reader users who were navigating fine."
    claim: "You announce dynamic updates to screen readers — ariaNotify() (WAI-ARIA 1.3) is the coming replacement for aria-live hacks, but it's Firefox-only as of mid-2026 and invites alert()-style over-narration; keep semantic signals first."
    applies_when: { platforms: [react] }
  - title: "Accessibility in React: Common Mistakes and How to Fix Them"
    url: "https://certificates.dev/blog/accessibility-in-react-common-mistakes-and-how-to-fix-them"
    by: "Aurora Scharff"
    what: "A durable React a11y reference from a recognized expert: semantic HTML, labeling, focus management on route/modal changes, ARIA live regions, with concrete useId/useRef patterns and a WCAG-cited checklist."
  - title: "AI-Generated UI Is Inaccessible by Default"
    url: "https://frontendmasters.com/blog/ai-generated-ui-is-inaccessible-by-default/"
    by: "Frontend Masters"
    what: "Why LLM-generated components ship empty accessibility trees, and a five-layer enforcement system (prompt constraints, jsx-a11y lint, axe-core runtime tests, CI gates, headless primitives). Durable guidance as AI codegen becomes the default."
detect_source:   # source-level signals owned by this entry (regex over repo source)
  - pattern: "accessibilityLabel|accessibilityRole|aria-label|aria-labelledby|<[a-z]+[^>]*\\srole="
    absent: true
    min_stage: "production"
    signal: "no accessibility annotations found anywhere in source (production-stage repo)"
    hint: "screen-reader users get an empty tree — start with roles/labels on interactive elements and role-based test queries (see reading: 'Accessibility in React'); depth → design-systems-governance"
